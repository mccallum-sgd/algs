import java.util.Comparator;

import edu.princeton.cs.algs4.MinPQ;

public class MyHeapSort<Item> {
    
    public void minSort(Item[] a, Comparator<Item> comparator) {
        MinPQ<Item> q = new MinPQ<Item>(comparator);
        for (Item i: a)
            q.insert(i);
            
    }
}
